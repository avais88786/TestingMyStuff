@model IEnumerable<OTOMCollapse.Models.PropertyOwners>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProposerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatePropertyBuilt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProposerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatePropertyBuilt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PropertyOwnersId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PropertyOwnersId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PropertyOwnersId })
        </td>
    </tr>
}

</table>
