@model OTOMCollapse.Controllers.GroupContainer

@using (Html.BeginForm())
{
    @Html.DisplayFor(model => model.repeatGroups);
    
    @Html.EditorFor(model => model.repeatGroups[0]);

    <br/>
    @Html.DisplayFor(model => model.repeatGroups);
    
    @Html.TextBox("repeatGroups[2].id");
    
    
    <input type="submit" value="submit" />   
}
